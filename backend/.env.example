# --- Database ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=bank
DB_PASSWORD=bank123
DB_NAME=banking

# --- Flask ---
SECRET_KEY=change-me
FLASK_ENV=development
FLASK_PORT=5000
LARGE_TX_THRESHOLD=50000

# --- JWT ---
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRY=86400

# --- SMTP (leave empty to use mock email mode) ---
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@bank.local
